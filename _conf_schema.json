{
    "temp_dir": {
        "description": "临时文件保存目录",
        "type": "string",
        "default": "data/plugin_data/astrbot_plugin_meme_grabber"
    },
    "delete_after_send": {
        "description": "发送后删除临时文件",
        "type": "bool",
        "default": true,
        "hint": "注意:开启时，也会在插件重载后立即删除所有临时文件"
    },
    "default_extension": {
        "description": "默认表情包扩展名",
        "type": "string",
        "default": "jpg",
        "hint": "当无法检测表情包类型时使用的默认扩展名"
    },
    "download_timeout": {
        "description": "表情包下载超时时间（秒）",
        "type": "int",
        "default": 60
    }
}


